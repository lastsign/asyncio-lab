# asyncio-lab
event loop and concurrency

The goal of this repository is essentially to try to figure out how event loop is actually works

I think I'm getting an alergic to callbacks

Generator implementation is much straightforward than callback one
The code looks almost the same, identical

Even the tricks with task switching there current coroutine just disappear let control for other tasks (Scheduler.sleep)
